export interface ITemplate {
    templateId: number,
    templateName: string,
    templatePosition: string,
    templateSkills: Array<string>,
    templateSitizen: string,
    templateSity: string,
    templateSalary: Array<number>,
    templateAge: Array<number>,
    templateExp: Array<number>
}